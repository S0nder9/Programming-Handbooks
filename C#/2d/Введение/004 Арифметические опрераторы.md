## 004 Арифметические опрераторы

```csharp

 float res;
 int x, y;

 x = 5;
 y = 2;

//  res = x / y; // Получается 2, так как оба операнда целые числа, и результат будет целочисленным делением.
              // Чтобы получить правильный результат, нужно привести хотя бы один из операндов к типу float:
 res = Convert.ToSingle(x) / y; // Теперь результат будет 2.5, так как один из операндов является числом с плавающей запятой.

 int timeInMins = 130;
 int hour;
 int minute;

 hour = timeInMins / 60; // Получаем количество часов, используя целочисленное деление
 minute = timeInMins % 60; // Получаем остаток от деления, который будет количеством минут

 int age = 18;
 age = age + 1; // Одно и то же
 age++;// Одно и то же
 age += 1;// Одно и то же

 int res2 = 5 + (12 - 5) * 3 - 1;

```
