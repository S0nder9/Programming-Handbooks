/**
 * 003 - Знакомство с TypeScript
 * =============================
 * 
 * Первое приложение на TypeScript
 * -------------------------------
 * В этом уроке мы создадим простое консольное приложение на TypeScript.
 * Оно поможет разобраться с основами работы с файлами .ts, их компиляцией и запуском.
 */

/**
 * 1. Создание проекта
 * -------------------
 * Для начала создадим новую папку для проекта и перейдём в неё:
 * 
 *   mkdir my-first-ts-app && cd my-first-ts-app
 * 
 * Далее инициализируем TypeScript-проект командой:
 * 
 *   tsc --init
 * 
 * Эта команда создаст файл tsconfig.json с настройками компиляции TypeScript.
 */

/**
 * 2. Создание основного файла
 * ---------------------------
 * Теперь создадим новый файл index.ts и добавим в него базовый код:
 */

// Объявляем переменную с типом string
let message: string = "Привет, TypeScript!";

// Функция для вывода сообщения в консоль
function greet(name: string, age: number): string {
    return `Привет, ${name}! Тебе ${age} лет. Добро пожаловать в TypeScript.`;
}

// Вызываем функцию и передаем в неё строку и число
console.log(greet("Никита", 25));

/**
 * 3. Компиляция и запуск кода
 * ----------------------------
 * Чтобы скомпилировать TypeScript-код в JavaScript, используйте команду:
 * 
 *   tsc index.ts
 * 
 * После выполнения этой команды появится файл index.js.
 * Запустите его с помощью Node.js:
 * 
 *   node index.js
 * 
 * В консоли должно появиться сообщение: "Привет, Никита! Тебе 25 лет. Добро пожаловать в TypeScript."
 */

/**
 * 4. Автоматическая компиляция (watch mode)
 * -----------------------------------------
 * Чтобы не компилировать код вручную после каждого изменения, можно запустить компилятор в режиме наблюдения:
 * 
 *   tsc --watch
 * 
 * Теперь при каждом сохранении изменений файлы .ts будут автоматически перекомпилированы.
 */

/**
 * Заключение
 * ----------
 * Поздравляем! 🎉 Вы создали своё первое TypeScript-приложение.
 * Теперь вы знаете, как писать код, компилировать его и запускать в Node.js.
 * В следующих уроках мы углубимся в работу с типами, функциями, классами и интерфейсами.
 */
