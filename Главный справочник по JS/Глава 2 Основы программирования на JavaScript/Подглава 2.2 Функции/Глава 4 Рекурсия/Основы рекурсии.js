// Глава 4: Рекурсия

// Подглава 4.1: Основы рекурсии

// Что такое рекурсия
/*
Рекурсия - это процесс, в котором функция вызывает саму себя во время своего выполнения. 
Это позволяет решать задачи, которые могут быть разбиты на более мелкие подзадачи того же типа.
*/

// Примеры рекурсивных функций

// Пример 1: Факториал числа
function factorial(n) {
    if (n === 0) {
        return 1;
    }
    return n * factorial(n - 1);
}

console.log(factorial(5)); // Выведет: 120

/*
В этом примере функция factorial рекурсивно вызывает саму себя для расчета факториала числа n. 
Базовый случай, когда n равно 0, возвращает 1. 
В противном случае функция вызывает себя для вычисления факториала числа n - 1 и умножает результат на n.
*/

// Пример 2: Вычисление суммы элементов массива
function sumArray(arr) {
    if (arr.length === 0) {
        return 0;
    }
    return arr[0] + sumArray(arr.slice(1));
}

console.log(sumArray([1, 2, 3, 4, 5])); // Выведет: 15

/*
В этом примере функция sumArray рекурсивно вызывает саму себя для вычисления суммы элементов массива. 
Базовый случай, когда массив пустой, возвращает 0. 
В противном случае функция складывает первый элемент массива с результатом вызова себя для остальной части массива (без первого элемента).
*/
