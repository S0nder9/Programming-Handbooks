// Подглава 3.4: Поиск элементов

// Метод set()

const arr = [1, 2, 3, 4, 5];

arr.set(2, 6); // Заменяет элемент с индексом 2 на новое значение 6

console.log(arr); // Выведет: [1, 2, 6, 4, 5]

// set() предназначен для установки нового значения элемента по указанному индексу в массиве.