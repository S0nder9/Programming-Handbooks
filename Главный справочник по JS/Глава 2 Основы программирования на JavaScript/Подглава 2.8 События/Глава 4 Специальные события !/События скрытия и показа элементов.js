    // Событие скрытия элемента
element.addEventListener('hide', () => {
    console.log('Элемент был скрыт');
});

// Событие показа элемента
element.addEventListener('show', () => {
    console.log('Элемент был показан');
});
