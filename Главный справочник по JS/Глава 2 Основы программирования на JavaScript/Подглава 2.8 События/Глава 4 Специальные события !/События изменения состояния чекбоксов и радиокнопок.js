// Событие изменения состояния чекбокса
checkbox.addEventListener('change', () => {
    console.log('Состояние чекбокса изменилось');
});

// Событие изменения состояния радиокнопки
radio.addEventListener('change', () => {
    console.log('Состояние радиокнопки изменилось');
});
