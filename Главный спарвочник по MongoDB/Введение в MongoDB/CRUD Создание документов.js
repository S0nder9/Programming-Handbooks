// Глава 1: Введение в MongoDB - CRUD Создание документов в cmd

// MongoDB — это документно-ориентированная база данных, которая хранит данные в формате BSON (бинарное представление JSON).
// Основные операции в MongoDB включают создание, чтение, обновление и удаление данных, известные как CRUD (Create, Read, Update, Delete).
// В этой главе мы рассмотрим процесс создания документов в MongoDB через командную строку (cmd).

// 1. Установка MongoDB
// Прежде чем начать работать с MongoDB, убедитесь, что она установлена на вашем компьютере.
// Для запуска MongoDB используйте следующую команду в cmd:
// > mongod

// Запуск клиента MongoDB через командную строку:
// > mongo

// 2. Подключение к базе данных
// Для начала работы необходимо подключиться к базе данных или создать новую.
// В MongoDB по умолчанию используется база данных "test", но вы можете переключиться на другую или создать новую базу данных с помощью команды use:
> use myDatabase
// Эта команда создаст новую базу данных, если она не существует, и подключит вас к ней.

// 3. Создание коллекции
// В MongoDB документы хранятся в коллекциях, которые являются аналогом таблиц в реляционных базах данных.
// Для создания коллекции можно просто вставить первый документ в нее:
> db.createCollection('myCollection') // Создание коллекции (опционально)

// 4. Вставка документа в коллекцию
// Для создания документа используется метод `insertOne()` для вставки одного документа или `insertMany()` для вставки нескольких документов сразу.
// Пример вставки одного документа:
> db.myCollection.insertOne({
    name: "John Doe",
    age: 30,
    job: "Software Developer",
    location: "New York"
  })

// Эта команда вставит документ в коллекцию `myCollection` с полями `name`, `age`, `job` и `location`.

// Пример вставки нескольких документов:
> db.myCollection.insertMany([
    {
      name: "Alice",
      age: 25,
      job: "Designer",
      location: "Los Angeles"
    },
    {
      name: "Bob",
      age: 28,
      job: "Engineer",
      location: "San Francisco"
    }
  ])

// Эта команда вставит два документа в коллекцию `myCollection`.

// 5. Проверка созданных документов
// Чтобы убедиться, что документы были успешно созданы, можно использовать метод `find()` для получения всех документов из коллекции:
> db.myCollection.find().pretty()

// Эта команда выведет все документы из коллекции `myCollection` в удобочитаемом формате.

// Итог:
// Мы рассмотрели основные шаги для создания документов в MongoDB через командную строку.
// Сначала нужно подключиться к базе данных, создать коллекцию (опционально), а затем вставить документы с помощью методов `insertOne()` и `insertMany()`.
// После вставки данных можно проверить их с помощью метода `find()`.
