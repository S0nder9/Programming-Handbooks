# Глава 2: Форматирование кода Python и PEP8

# Форматирование кода Python и PEP8

# PEP8 (Python Enhancement Proposal 8) - это стиль кодирования для Python, который содержит рекомендации по оформлению кода.
# Соблюдение PEP8 помогает улучшить читаемость и консистентность кода, облегчая его сопровождение и совместную работу над проектом.

# Основные принципы PEP8:

# 1. Отступы
# Используйте 4 пробела на каждый уровень отступа. Не используйте табуляцию.
def my_function():
    for i in range(10):
        print(i)

# 2. Длина строки
# Ограничивайте длину строки 79 символами. Для длинных строк используйте перенос.
long_string = (
    "Это очень длинная строка, которая превышает 79 символов, "
    "поэтому она была разбита на несколько строк."
)

# 3. Пустые строки
# Отделяйте функции и классы двумя пустыми строками. Внутри классов методы отделяйте одной пустой строкой.
class MyClass:
    
    def method_one(self):
        pass

    def method_two(self):
        pass

# 4. Пробелы в выражениях и инструкциях
# Используйте пробелы вокруг операторов и после запятых для улучшения читаемости.
x = 1
y = 2
sum = x + y

# 5. Импорты
# Импорты должны быть в отдельных строках и расположены в начале файла.
import os
import sys

# Импорты стандартной библиотеки, сторонних библиотек и собственных модулей разделяйте пустыми строками.
import os
import sys

import numpy as np
import pandas as pd

from my_module import my_function

# Пример применения PEP8 в реальном коде
def calculate_area(radius):
    """Вычисляет площадь круга по заданному радиусу."""
    import math
    return math.pi * radius ** 2

def main():
    radius = 5
    area = calculate_area(radius)
    print(f"Площадь круга с радиусом {radius} составляет {area:.2f}")

if __name__ == "__main__":
    main()

# Итог
# Соблюдение рекомендаций PEP8 позволяет улучшить качество и читаемость кода, делает его более последовательным и простым для понимания другими разработчиками.
# Следование этим стандартам помогает поддерживать высокое качество кодовой базы и облегчает командную работу.
